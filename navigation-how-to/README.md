# Navigation How-To (Code First)

All practical guidance is inside Swift code comments.

Use `starter-code/Navigation/` in this order:

1. `Models/`
2. `Commands/`
3. `Core/`
4. `Flows/`
5. `Global/`
6. `Analytics/`

Each flow keeps its own router extension (`AppRouter+<Flow>Routing.swift`).
