import SwiftUI

// Copy this file into a fresh target, then rename the App struct to your product name.
@main
struct NavigationStarterApp: App {
    var body: some Scene {
        WindowGroup {
            RootCoordinatorView()
        }
    }
}
